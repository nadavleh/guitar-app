{
  "name": "chorect-web",
  "version": "1.6.0",
  "description": "Web port of Chorect — an interactive guitar/cavaquinho fretboard companion",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "verify": "vite build --ssr test/verify.ts --outDir dist-test --logLevel warn && node dist-test/verify.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
